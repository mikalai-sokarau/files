select - выборка.
distinct - только уникальные значения. Относится ко всему набору полей.
as - своё название для выбранной колонки.
round - округление.
trunk - отбрасывание дробной части.

Типы данных:
  # Строки: ||, length, сhar, upper, lower
  # Дата: sysdate (текущая дата), extract(выбрать одну состовляющую даты, возвращает число), 
  to_char(меняет формат отображения)
  # Число: 

insert - вставка.


